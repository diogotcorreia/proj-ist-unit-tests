// even though the number below minus 1 can be represented
// in 4 bytes, MML only considers positive literals
// and then applies the unary neg operation,
// so this is the smallest number that doesn't overflow.
int i = -2147483647; // -(2^31 - 1)
begin
  i!!
  return 0;
end
